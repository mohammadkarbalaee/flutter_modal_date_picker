import 'package:flutter/material.dart';

double fullwidthButtonHeight = 0.06;

BorderRadius fullwidthButtonRadius(double radius) {
 return BorderRadius.circular(radius);
}

BorderSide fullwidthButtonOutline(Color color) {
  return BorderSide(color: color);
}